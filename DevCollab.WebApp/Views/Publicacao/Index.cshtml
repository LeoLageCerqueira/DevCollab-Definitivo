@model IEnumerable<DevCollab.Domain.Entities.Publicacao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Texto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Autor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Texto)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Autor.Nome)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.IdPublicacao">Edit</a> |
                    @*<a asp-action="Details" asp-route-id="@item.IdPublicacao">Details</a> |*@
                    <a asp-action="Delete" asp-route-id="@item.IdPublicacao">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
